---

spring:
  application:
    name: roster
  cloud:
    config:
     uri: ${vcap.services.configserver.credentials.uri}

management:
  security:
    enabled: false

security:
  basic:
    enabled: false
